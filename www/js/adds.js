var item;

$(document).ready(function()
{
   // item = window.localStorage.getItem("add");
    //console.log("item:" + item + ";");
    $(this).item("id") 
})

function goBack() {
    window.history.back();
}